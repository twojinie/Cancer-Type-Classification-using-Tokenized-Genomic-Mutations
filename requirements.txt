# Install PyTorch separately based on your CUDA/CPU:
# See: https://pytorch.org/get-started/locally/
# Example (CPU-only):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

requests>=2.28,<3
pandas>=2.0,<3
numpy>=1.24,<3
tqdm>=4.65,<5
matplotlib>=3.7,<4
scikit-learn>=1.3,<2

# NLP / Transformers
transformers>=4.41,<5
sentencepiece>=0.1.99,<0.2

# XGBoost (GPU optional, CPU works)
xgboost>=2.0,<3

# Excel support for pandas.read_excel (if needed)
openpyxl>=3.1,<4

# Notebooks (optional but recommended)
jupyter>=1.0,<2

